## 5. 剧集管理模块（作品管家）

### 5.1 作品库全景图
剧集管理模块是用户个人创作资产的“档案馆”，通过科学的层级架构（项目-剧集-分镜）对所有内容进行组织。

### 5.2 顶部工具栏深度操作
#### 5.2.1 智能检索与过滤
*   **组合搜索**：支持“关键词 + 状态”的组合筛选。例如，用户可以仅查看“已完成”状态且标题包含“重生”的项目。
*   **实时响应**：搜索采用防抖（Debounce）技术，在用户停止输入 300ms 后自动刷新结果，极大提升了流畅度。

#### 5.2.2 视图模式的场景化应用
*   **网格模式 (Grid)**：主要用于视觉审查。卡片采用 16:10 黄金比例，能够清晰展示剧集封面及其美学风格。
*   **列表模式 (List)**：主要用于效能管理。列表模式提供了横向的详细字段，包括 ID（唯一识别码）、最后更新时间、总点击量、收藏量等。点击列头可进行正序/倒序排列。

### 5.3 剧集卡片交互细节
#### 5.3.1 状态徽标含义
*   **草稿 (Grey)**：基础元数据已建立，但尚未启动任何生成任务。
*   **制作中 (Blue)**：正在进行剧本扩写、分镜绘图或视频渲染。
*   **已发布 (Green)**：全集视频已通过审核并可供导出。

#### 5.3.2 封面动态管理
系统提供了灵活的封面解决方案：
*   **手动上传**：点击“上传封面”，支持拖拽上传本地设计的 `.jpg` 或 `.png` 文件。
*   **AI 智能生成**：通过集成“封面工坊”，用户只需输入简单的 Prompt，系统即可生成极具视觉冲击力的电影感海报。

### 5.4 作品删除的安全边界
*   **二次确认流程**：点击删除按钮后，系统会弹出全局模态对话框。
*   **数据隔离逻辑**：为了确保护数据隐私，一旦用户点击“确认删除”，该作品关联的所有 AI 训练模型参数、中间生成的低清晰度素材及高清成片均会进入 7 天“回收站”期（企业版特有）或直接物理抹除。

---

## 6. 剧本创作模块（AI 编剧室）

### 6.1 三步走创作流
剧本模块将复杂的文学创作拆解为三个标准化步骤：**剧本设定 -> 大纲生成 -> 剧本正文**。

### 6.2 剧本参数精细化控制
左侧面板提供了剧本的“灵魂参数”设定：
*   **题材库**：涵盖了当前短剧市场最热门的 10 余种题材，如“爽文逆袭”、“豪门恩怨”、“古穿今”等。
*   **风格倾向**：通过调节风格滑块，可以控制文本的“幽默度”、“虐心度”或“热血度”。
*   **受众定位**：明确剧本是面向“银发族”、“Z世代”还是“下沉市场”，AI 将自动调整台词的通俗易懂程度。

### 6.3 核心编辑台交互
#### 6.3.1 模块化文本块
右侧编辑区由多个独立文本块组成，包括：
*   **世界观 (World Setting)**：定义物理规则、社会等级。
*   **金手指 (Cheat)**：设定主角的核心竞争优势。
*   **角色档案 (Characters)**：记录角色姓名、年龄、性格、甚至标志性台词。

#### 6.3.2 局部 AI 呼叫功能
每个文本块右上角均有一个“MagicStick”图标。点击后，AI 将仅针对该特定模块进行创作。例如，若用户对现有的“世界观”不满意，可单独点击该按钮进行重写，而不会影响其他模块。

### 6.4 智能补全与从零生成
*   **基于设定补全**：这是最高效的模式。用户填入姓名和一句话简介，点击补全，AI 将自动推导人物关系与大纲细节。
*   **全案生成**：适合纯小白用户，由 AI 主导整个创意策划。

---

## 7. 主体设置模块（资产中心）

### 7.1 视觉一致性维护 (Visual Consistency)
本模块的核心价值在于通过“主体提取”技术，固定剧中核心元素的视觉特征，从而解决 AI 生成视频中常见的“串脸”或“场景突变”问题。

### 7.2 分类资产库管理
#### 7.2.1 角色库 (Character Library)
*   **基准图设定**：用户可以为每个角色生成一张“正脸基准图”。
*   **特征描述词**：AI 会自动提取角色的“长发、蓝色眼睛、职业装”等标签，并在后续分镜生成中强制锁定这些标签。

#### 7.2.2 场景库 (Scene Library)
*   **氛围渲染**：定义场景的光影基调（如“赛博朋克霓虹光”、“午后温馨自然光”）。
*   **关键地标**：固定场景中的核心物品摆放。

#### 7.2.3 道具库 (Prop Library)
*   **特写预设**：对于剧情中的关键道具（如神秘吊坠、契约书），进行高精细度的特征建模描述。

### 7.3 AI 自动规划流
1.  **解析剧本**：系统自动读取上一步生成的剧本正文。
2.  **主体识别**：识别出文中出现的专有名词。
3.  **批量绘图**：一键为所有主体生成 4 张可选图。

### 7.4 资产版本回溯
每个资产卡片上都会显示“历史版本数”。点击后进入“历史看板”，用户可以随时找回 3 天前生成的某个角色形象，并一键将其应用为“主版本”。

### 7.5 流转逻辑锁
为了确保下一阶段视频生成的成功率，本模块设有“逻辑锁”：必须至少完成 1 个角色和 1 个场景的设定，且图片生成状态正常，方可解锁“分镜视频”模块。
