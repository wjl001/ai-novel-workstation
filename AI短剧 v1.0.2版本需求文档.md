# AI 短剧工作站 v1.0.2 版本需求文档

## 1. 修订记录
| 版本号 | 修订日期 | 修订内容 | 修订人 |
| --- | --- | --- | --- |
| v1.0.0 | 2026-07-20 | 初始版本需求文档 | 产品组 |
| v1.0.2 | 2026-08-20 | 模型平台增强、主体设置优化、分镜视频功能增强及 UI/UX 优化 | AI 助手 |

## 2. 项目背景
AI 短剧工作站旨在为创作者提供从剧本构思到视频合成的全流程 AI 创作工具。v1.0.2 版本侧重于提升模型平台的安全性与多样性、优化主体设置的精细化控制、增强分镜视频的生成反馈及用户体验。

## 3. 核心功能模块需求

### 3.1 模型平台
*   **敏感词拦截（一期）**
    *   **需求描述**：在模型平台接入敏感词过滤机制，对用户输入的剧本标题、大纲、分镜描述等内容进行实时检测。
    *   **功能逻辑**：
        *   建立基础敏感词库（政治、色情、暴力等）。
        *   触发拦截时，前端弹出警告提示，阻止非法内容发送至 AI 模型。
        *   记录拦截日志，用于后续分析。
*   **模型接入增强**
    *   **新增模型**：接入字节跳动 `doubao-seedance-2-0` 模型。
    *   **版本细分**：支持 `fast`（快速预览版）和 `mini`（轻量版）两种配置，满足不同场景下的生成速度与成本需求。
    *   **代码参考**：需在 `src/config/models.ts` 中新增模型配置。

### 3.2 主体设置
*   **主体管理优化**
    *   **新增/删除功能**：在主体库（`SubjectLibraryModal.vue`）中增加显式的主体新增和删除操作，方便用户管理自定义角色和场景。
*   **角色音频参考**
    *   **需求描述**：新增角色时，支持上传音频文件或提供音频链接作为语音克隆/音色参考。
    *   **UI 变动**：`SubjectEditDialog.vue` 中增加“音频参考”上传项。
*   **分辨率配置**
    *   **需求描述**：为主体图片生成增加分辨率选择（如 512x512, 1024x1024 等）。
*   **输入项灵活化**
    *   **需求描述**：对于站外上传的图片，其对应的文字描述支持“不必填”，提升上传效率。
*   **异步生成图片**
    *   **需求描述**：将主体图片的生成过程由同步阻塞改为异步处理。
    *   **功能逻辑**：用户点击生成后，系统进入“生成中”状态，用户可进行其他操作，生成完成后通过消息通知或状态更新告知用户。
*   **场景图片自动同步**
    *   **需求描述**：场景图片生成后，默认同步至视频资产库（AssetsView），无需手动点击同步。

### 3.3 分镜视频
*   **生成失败原因反馈**
    *   **需求描述**：批量生成视频时，若某一条分镜生成失败，需在界面上显示具体的失败原因（如：算力不足、触发敏感词、模型超时等）。
*   **分辨率记忆功能**
    *   **需求描述**：系统应记录用户上一次选择的视频分辨率，并在下次生成时默认选中。
*   **算力豆消耗归集**
    *   **需求描述**：在生成视频前的文本格式转换（AI 优化描述词等）所消耗的算力豆，应统一计入视频生成的总消耗记录中，方便用户核算。
*   **样式布局调整**
    *   **需求描述**：对分镜配置界面的样式和布局进行优化，提升交互流畅度（对应 `StoryboardView.vue`）。
*   **全集合成进度简化**
    *   **需求描述**：合成全集视频时，将原本多任务并发的进度条简化为单一的总百分比进度显示。

### 3.4 首页与创作辅助
*   **热门题材算力保护**
    *   **需求描述**：首页“热门题材”模块支持屏蔽用户扣除算力豆功能，该部分消耗默认由平台承担（平台损耗），以吸引新用户尝试。
*   **视频比例扩充**
    *   **需求描述**：灵感生成页面新增 9:16（竖屏）和 16:9（横屏）比例支持。
*   **创作耗时预估**
    *   **需求描述**：在剧本创作（`NovelGenerator.vue`）生成大纲的过程中，在 AI 剧本构思阶段新增“预估剩余时间”显示，缓解用户等待焦虑。

### 3.5 会员中心
*   **充值记录展示**
    *   **需求描述**：在会员中心（`MemberCenterView.vue`）新增“充值记录”子模块，展示用户的历史订单、金额、支付状态及获得的算力豆明细。

## 4. 非功能性需求
*   **性能**：异步任务处理需保证状态更新的及时性。
*   **安全性**：敏感词过滤需覆盖所有用户输入入口。
*   **易用性**：分辨率记忆功能需实现本地持久化存储。

## 5. 验收标准
1.  成功接入 `doubao-seedance-2-0` 并能正常生成。
2.  主体设置中音频参考上传正常，分辨率配置生效。
3.  视频生成失败原因显示正确。
4.  会员中心能正确查看到充值记录。
5.  剧本创作生成大纲时有明确的预估时间提示。
